# tor.txt — seed file for Tor exit nodes
#
# Применяется ТОЛЬКО если BLOCK_TOR=1 в /etc/shieldnode/shieldnode.conf
# (или через guard settings menu).
#
# Основной источник — официальный Tor Project list, качается каждый час:
#   https://check.torproject.org/torbulkexitlist
#
# Сюда добавлять обычно не надо — Tor Project сам поддерживает актуальный
# список. Файл существует для оффлайн-режима или forked-репо.
#
# Формат: один IP или CIDR на строку. Комментарии через #.
