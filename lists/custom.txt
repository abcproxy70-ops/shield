# shieldnode custom blocklist (seed)
# nft set: custom_blocklist_v4
#
# Этот файл — личный список оператора. По умолчанию URL-источников НЕТ:
# работает только то что лежит здесь.
#
# Path-watcher (inotify) триггерит обновление сразу при изменении файла —
# изменения подхватываются за <1 секунды.
#
# На сервере:
#   echo '198.51.100.42' | sudo tee -a /etc/shieldnode/lists/custom.txt
#
# Один IP или CIDR на строку, # = комментарий.
#
# === Сюда залей свой список из 880 IPs (содержимое txt.txt от пользователя) ===
